{
  "name": "grug-cli-core",
  "version": "0.1.0",
  "description": "Headless Grug Code CLI and Stdio MCP Daemon",
  "type": "module",
    "scripts": {
    "test": "vitest run",
    "build:daemon": "bun build --compile src/daemon.ts --outfile dist/grug-daemon",
    "build:cli": "bun build --compile src/cli.ts --outfile dist/grug",
    "build": "bun run build:daemon && bun run build:cli",
    "mcp-config": "bun run src/mcp-config-generator.ts"
  },
  "dependencies": {
    "@ai-sdk/google": "^1.0.0",
    "@ai-sdk/openai": "^1.1.0",
    "@clack/prompts": "^0.9.1",
    "@modelcontextprotocol/sdk": "^1.2.9",
    "ai": "^4.0.0",
    "effect": "^3.16.12",
    "tree-sitter-wasms": "^0.1.13",
    "web-tree-sitter": "^0.22.4",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.8.6",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}